1. Make sure `RethinkDB` is up and running

2. `git clone https://github.com/tenzan/react-rethinkdb-tutorial.git`

3. `cd react-rethinkdb-tutorial` && `npm install`

4. `npm start`

5. Open `localhost:8015`
